
let initialState = {}
const ActionType = {}

export const sidebarReducer = (state = initialState , action: any): any=> {
    switch (action.type) {

        default: {
            return state
        }
    }

}

